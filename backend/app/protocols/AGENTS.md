# Protocols Module

Owns filesystem-backed first-aid protocol packs.

## Rules

- Keep packs human-editable and git-friendly.
- Prefer simple JSON + Markdown over heavier storage for v1.
- Protocol packs define searchability and checklist templates, not model logic.
